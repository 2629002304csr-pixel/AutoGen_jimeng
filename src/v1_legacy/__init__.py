"""v1 legacy code, deprecated.

This package contains the original M3-M20 implementation. Kept for
backward compatibility (old tests, old CLI `python main.py --web`).

For new work, use the top-level `src/` package (M21 v2 rewrite).
"""
